<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Details</title>
    <style>
    body {
        font-family: "Monoid", monospace;
        background-color: #222;
        color: #fff;
        margin: 0;
        padding: 0;
    }
    
    h1 {
        margin: 20px;
        font-size: 20px;
        text-align: left;
    }
    
    .row {
        margin: 20px;
    }
    
    p {
        margin-bottom: 10px;
        text-align: left;
    }
    
    .button-container a {
        color: #ff9900;
        text-decoration: none;
        background-color: #333;
        padding: 5px;
        display: inline-block;
        border-radius: 5px;
    }
    
    .button-container a:hover {
        background-color: #444;
    }
</style>
</head>
<body>
    <div class="jumbotron">
        <h1>Book Information</h1>
    </div>
    <div class="row">
        <p>Book Name: <% if (typeof (item.bookname) !== "undefined") { %><%= item.bookname %><% } %></p>
        <p>ISBN: <% if (typeof (item.isbn) !== "undefined") { %><%= item.isbn %><% } %></p>
        <p>Category: <% if (typeof (item.category) !== "undefined") { %><%= item.category %><% } %></p>
        <p>Publisher: <% if (typeof (item.publisher) !== "undefined") { %><%= item.publisher %><% } %></p>
        <p>Author: <% if (typeof (item.author) !== "undefined") { %><%= item.author %><% } %></p>
        <p>Remark: <% if (typeof (item.remark) !== "undefined") { %><%= item.remark %><% } %></p>
    </div>
    <div class="button-container">
        <a href="/edit?_id=<%= item._id %>">Update the information</a>
        <a href="/delete?_id=<%= item._id %>">Delete the information</a>
        <a href="/home">Home</a>
    </div>
</body>
</html>

